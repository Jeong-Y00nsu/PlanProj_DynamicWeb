function init(){
    //날짜 오늘 날짜로 설정
    //
    /*int year;
    int month;
    for (int i=0;i<cntOfWeeks;i++){
        for(int j=0;j<7;j++){
            int day = 7 * i + j - firstDayOfWeek + 1;
            if(day > 0 && day <= lastDay) {
                //<td> 안에 날짜 넣기
                if(plan[day-1]["yyyyMMdd"] != null && plan[day-1]["yyyyMMdd"].size() > 0){
                    // 일정 표시
                    // 일정 size()개
                }
            }
        }
    }*/
}

$(document).ready(function(){
    init();
});